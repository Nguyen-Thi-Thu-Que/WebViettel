/**
 * Viettel Mobile Package Management Backend API (Skeleton)
 * 
 * Đây là phần khung của Backend dự án Monorepo. 
 * Phần này hiện tại chưa kết nối cơ sở dữ liệu hay business logic.
 * Tất cả dữ liệu hiện tại đều chạy qua Mock Data ở phía Frontend (Client).
 * 
 * Các API Route cần xây dựng trong tương lai:
 * - POST /api/auth/login : Đăng nhập
 * - POST /api/auth/register : Đăng ký
 * - GET /api/packages : Lấy danh sách gói cước
 * - GET /api/packages/:id : Chi tiết gói cước
 * - POST /api/packages : CRUD Gói cước (Admin)
 * - POST /api/transactions/deposit : Nạp tiền
 * - POST /api/transactions/subscribe : Đăng ký gói cước
 * - GET /api/chatbot/config : Lấy prompt và từ khóa chatbot
 */

console.log("Viettel Backend Skeleton is initialized. API server is not active.");
